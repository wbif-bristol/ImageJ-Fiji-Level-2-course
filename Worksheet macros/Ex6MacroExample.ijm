

x1=getResult("X",0);
y1=getResult("Y",0);

x2=getResult("X",1);
y2=getResult("Y",1);


distance= sqrt((x2-x1)*(x2-x1) + (y2-y1)*(y2-y1));

print("the distance between the two nuclei is: "+distance+" pixels");